Decision:

The Employee class is responsible for validating its own data.
An Employee object must never exist in an invalid state.
Validation logic will be encapsulated in dedicated validation methods rather than being embedded directly in __init__().

Reason:

Prevent invalid objects.
Centralize business rules.
Avoid duplicated validation logic.
Improve maintainability.